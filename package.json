{
    "name": "@scom/contract-deployer",
    "version": "0.1.8",
    "engines": {
        "node": ">=18.0.0"
    },
    "main": "dist/scconfig.json",
    "dependencies": {
        "@ijstech/components": "^0.1.29",
        "@ijstech/eth-contract": "^0.1.4",
        "@ijstech/eth-wallet": "^0.1.26"
    },
    "devDependencies": {
        "@ijstech/cli": "^0.1.19",
        "@ijstech/compiler": "^0.1.16",
        "@scom/dapp": "^0.1.6"
    },
    "scripts": {
        "build": "rm -rf dist && npx @ijstech/compiler",
        "serve": "npx @ijstech/cli serve 8080",
        "test": "npm run build && npm run serve"
    }
}